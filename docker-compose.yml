version: '3.8'

networks:
  app-network:
    driver: bridge

services:
  # Backend service — uses host network so it can reach external DB at 10.40.93.4
  app:
    container_name: portal-emendas-backend
    build:
      context: .
      dockerfile: src/main/docker/Dockerfile.jvm
    ports:
      - "8085:8080"
    networks:
      - app-network
    restart: on-failure:5
    env_file:
      - .env
    volumes:
      - /data2/app-emendas:/data2/app-emendas
    environment:
      UPLOAD_DIR: /data2/app-emendas
      QUARKUS_DATASOURCE_DB_KIND: postgresql
      QUARKUS_HIBERNATE_ORM_DATABASE_GENERATION: none
      QUARKUS_FLYWAY_MIGRATE_AT_START: "true"
      QUARKUS_FLYWAY_BASELINE_ON_MIGRATE: "true"
      QUARKUS_FLYWAY_BASELINE_VERSION: "0"
      QUARKUS_FLYWAY_LOCATIONS: "classpath:db/migration"
      QUARKUS_FLYWAY_CLEAN_AT_START: "false"
      QUARKUS_FLYWAY_REPAIR_AT_START: "true"
      QUARKUS_FLYWAY_IGNORE_MISSING_MIGRATIONS: "true"
      QUARKUS_FLYWAY_IGNORE_IGNORED_MIGRATIONS: "true"
      QUARKUS_HTTP_PORT: 8080
      QUARKUS_HTTP_HOST: 0.0.0.0
      QUARKUS_SWAGGER_UI_PATH: /swagger-ui
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/q/health/ready || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # Frontend service — bridge network, proxies /api to host:8085
  frontend:
    container_name: portal-emendas-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "3005:80"
    networks:
      - app-network
    depends_on:
      - app
    restart: unless-stopped
